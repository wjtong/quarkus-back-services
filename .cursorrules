# CRM 服务端项目 - Cursor Rules

## 项目概述
这是一个基于 Quarkus 框架构建的 CRM (客户关系管理) 系统服务端项目，提供完整的 RESTful API 用于管理客户数据，并集成了 LangChain4j 提供 AI Agent 功能。

## 技术栈规范

### 核心框架
- **Quarkus**: 使用 Quarkus 框架作为主要开发框架
- **Java 版本**: 使用 JDK 17+ 进行开发
- **构建工具**: Maven 3.8+ 或 Gradle

### API 开发
- **REST API**: 使用 RESTEasy Reactive 构建响应式 RESTful API
- **JSON 处理**: 使用 RESTEasy Reactive Jackson 进行 JSON 序列化/反序列化
- **API 文档**: 使用 Quarkus SmallRye OpenAPI 自动生成 API 文档

### 数据层
- **ORM**: 使用 Hibernate ORM with Panache 简化数据库操作
- **数据库**: PostgreSQL 作为主数据库
- **数据库迁移**: 使用 `quarkus.hibernate-orm.database.generation=update` 自动更新表结构

### AI 集成
- **LangChain4j**: 使用 LangChain4j 框架提供 AI Agent 功能
- **大语言模型**: 支持多种 LLM 提供商 (OpenAI, Anthropic, Google, 本地模型等)
- **向量数据库**: 集成向量数据库用于语义搜索和 RAG (检索增强生成)
- **AI Agent**: 提供智能客服、数据分析、内容生成等 AI 功能

### 安全
- **认证**: 使用 SmallRye JWT 进行基于 JWT 的声明式安全认证
- **授权**: API 端点受 JWT 保护，需要在 Header 中提供 `Authorization: Bearer <token>`

## 代码规范

### 项目结构
- 遵循 Quarkus 标准项目结构
- 实体类放在 `src/main/java/` 下的适当包中
- 资源类 (REST 控制器) 使用 JAX-RS 注解
- AI 相关类放在 `src/main/java/com/example/crm/ai/` 包下
- 配置文件统一放在 `src/main/resources/application.properties`

### API 开发规范
- 所有 REST 端点必须使用适当的 HTTP 方法 (GET, POST, PUT, DELETE)
- 使用 JAX-RS 注解 (`@GET`, `@POST`, `@PUT`, `@DELETE`, `@Path`, `@Consumes`, `@Produces`)
- 为所有 API 端点添加 OpenAPI 注解 (`@Operation`, `@ApiResponse`, `@Tag`)
- 使用适当的 HTTP 状态码和错误处理

### 实体类规范
- 使用 Hibernate Panache 实体基类 (`PanacheEntity`)
- 为实体类添加适当的 JPA 注解 (`@Entity`, `@Table`, `@Column`)
- 使用 Lombok 注解简化代码 (`@Data`, `@NoArgsConstructor`, `@AllArgsConstructor`)

### AI 开发规范
- AI Agent 类必须使用 `@ApplicationScoped` 注解进行依赖注入
- 使用 LangChain4j 的 `AiServices` 创建 AI 服务实例
- AI 工具类必须实现 `Tool` 接口并添加 `@Tool` 注解
- 为 AI 服务添加适当的错误处理和日志记录
- AI 相关的配置参数统一在 `application.properties` 中管理
- 使用 `@RegisterForReflection` 注解确保 AI 相关类在原生编译时可用

### 安全规范
- 受保护的端点必须使用 `@RolesAllowed` 或 `@PermitAll` 注解
- JWT 配置必须在 `application.properties` 中正确设置
- 公钥文件应放在 `META-INF/resources/` 目录下
- AI API 端点需要适当的权限控制，避免敏感数据泄露

## 开发环境配置

### 数据库配置
```properties
# PostgreSQL 数据源配置
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=your-db-username
quarkus.datasource.password=your-db-password
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/crm_db

# 数据库表结构自动更新
quarkus.hibernate-orm.database.generation=update
```

### JWT 配置
```properties
# SmallRye JWT 配置
mp.jwt.verify.publickey.location=META-INF/resources/publicKey.pem
mp.jwt.verify.issuer=https://your-issuer.com/
```

### AI 配置
```properties
# LangChain4j 配置
langchain4j.openai.api-key=${OPENAI_API_KEY:your-openai-api-key}
langchain4j.openai.base-url=${OPENAI_BASE_URL:https://api.openai.com/v1}
langchain4j.openai.model=${OPENAI_MODEL:gpt-3.5-turbo}
langchain4j.openai.temperature=${OPENAI_TEMPERATURE:0.7}
langchain4j.openai.max-tokens=${OPENAI_MAX_TOKENS:1000}

# 向量数据库配置 (可选)
langchain4j.embedding.store.type=${EMBEDDING_STORE_TYPE:in-memory}
langchain4j.embedding.store.dimension=${EMBEDDING_DIMENSION:1536}

# AI Agent 配置
ai.agent.enabled=${AI_AGENT_ENABLED:true}
ai.agent.max-conversation-history=${AI_AGENT_MAX_HISTORY:10}
ai.agent.timeout=${AI_AGENT_TIMEOUT:30}
```

## 开发命令

### 启动开发服务器
```bash
# 使用 Maven
./mvnw quarkus:dev

# 使用 Gradle
./gradlew quarkusDev
```

### 构建项目
```bash
# 使用 Maven
./mvnw package

# 使用 Gradle
./gradlew build
```

## API 文档访问
- **Swagger UI**: http://localhost:8080/q/swagger-ui
- 所有 API 端点、请求参数和响应模型都会自动生成文档
- **AI Agent 端点**: `/api/ai/*` - AI 相关的 REST API 端点

## 最佳实践

### 错误处理
- 使用适当的异常处理机制
- 返回有意义的错误消息和 HTTP 状态码
- 记录重要的错误信息

### 性能优化
- 利用 Quarkus 的响应式特性
- 使用适当的数据库查询优化
- 考虑使用缓存机制

### 测试
- 为所有 API 端点编写单元测试和集成测试
- 使用 Quarkus 测试框架进行测试
- 确保测试覆盖率达到合理水平

### 日志记录
- 使用适当的日志级别
- 记录重要的业务操作和错误信息
- 避免记录敏感信息

### AI 最佳实践
- 为 AI 服务实现适当的缓存机制，减少 API 调用成本
- 使用流式响应处理长时间运行的 AI 任务
- 实现 AI 响应的内容过滤和审核机制
- 为 AI 工具添加输入验证和输出格式化
- 使用环境变量管理 AI 服务的 API 密钥
- 实现 AI 服务的降级策略，确保系统稳定性
- 定期监控 AI 服务的性能和成本

## 注意事项
- 这是一个纯后端项目，不包含任何前端页面
- 所有 API 都应该是 RESTful 的
- 确保所有配置都在 `application.properties` 中正确设置
- 开发时使用热重载功能提高开发效率
- AI 功能需要有效的 API 密钥才能正常工作
- 注意 AI 服务的成本控制，避免不必要的 API 调用
- 确保 AI 生成的内容符合业务需求和合规要求
- 在生产环境中使用适当的 AI 服务提供商和模型
